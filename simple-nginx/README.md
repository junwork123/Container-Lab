# nginx-certbot

## Requirements

- [x] Docker
- [x] Docker Compose
- [x] DNS Configurations
  - `A` - `www` - `${YOUR_SERVER_IP}`
  - `A` - `@` - `${YOUR_SERVER_IP}`

## Getting Started
1. Run `docker-compose`
```bash
docker compose up -d
```
